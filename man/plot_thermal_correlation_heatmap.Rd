% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/develop.R
\name{plot_thermal_correlation_heatmap}
\alias{plot_thermal_correlation_heatmap}
\title{Plot a thermal correlation heatmap}
\usage{
plot_thermal_correlation_heatmap(
  cor_result,
  use_adjusted = TRUE,
  show_significance = TRUE,
  sig_levels = c(0.05, 0.01, 0.001),
  sig_ns_label = "",
  low = "#3B82F6",
  mid = "white",
  high = "#EF4444",
  midpoint = 0,
  limits = c(-1, 1),
  tile_color = "grey85",
  tile_size = 0.4,
  rotate_x = 45,
  text_size = 4,
  na_fill = "grey95"
)
}
\arguments{
\item{cor_result}{A result object returned by `correlate_thermal_traits()`,
or a data.frame containing at least the columns:
`thermal_var`, `external_var`, `cor`, and optionally `p`, `p_adj`,
`significance`.}

\item{use_adjusted}{Logical. If `TRUE`, significance labels are based on
adjusted p-values (`p_adj`) when available. Default is `TRUE`.}

\item{show_significance}{Logical. If `TRUE`, significance labels are
displayed on the heatmap. Default is `TRUE`.}

\item{sig_levels}{Numeric vector of length 3 defining cutoffs for
`"*"`, `"**"`, and `"***"`, respectively. Default is
`c(0.05, 0.01, 0.001)`.}

\item{sig_ns_label}{Label used for non-significant results. Default is `""`.}

\item{low}{Color for negative correlations. Default is `"#3B82F6"`.}

\item{mid}{Color for zero correlation. Default is `"white"`.}

\item{high}{Color for positive correlations. Default is `"#EF4444"`.}

\item{midpoint}{Midpoint for the color scale. Default is `0`.}

\item{limits}{Numeric vector of length 2 for fill scale limits.
Default is `c(-1, 1)`.}

\item{tile_color}{Border color for heatmap tiles. Default is `"grey85"`.}

\item{tile_size}{Border line width for tiles. Default is `0.4`.}

\item{rotate_x}{Angle for x-axis text. Default is `45`.}

\item{text_size}{Font size for significance labels. Default is `4`.}

\item{na_fill}{Fill color for missing values. Default is `"grey95"`.}
}
\value{
A ggplot object.
}
\description{
Visualize pairwise correlations between thermal imaging-derived variables
and external traits as a heatmap.
}
\details{
This function is designed to work with the output of
`correlate_thermal_traits()`.
}
\examples{
# Assuming `res <- correlate_thermal_traits(...)`
# p <- plot_thermal_correlation_heatmap(res)
# print(p)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/develop.R
\name{plot_thermal_correlation}
\alias{plot_thermal_correlation}
\title{Plot correlation between one thermal feature and one external trait}
\usage{
plot_thermal_correlation(
  data,
  x,
  y,
  method = "spearman",
  group_var = NULL,
  use = "complete.obs",
  add_smooth = TRUE,
  smooth_method = "lm",
  point_size = 2.5,
  alpha = 0.8,
  show_stats = TRUE,
  digits = 3,
  annotation_x = 0.05,
  annotation_y = 0.95,
  xlab = NULL,
  ylab = NULL,
  title = NULL
)
}
\arguments{
\item{data}{A data.frame containing the variables to be plotted.}

\item{x}{A character string specifying the x-axis variable name.}

\item{y}{A character string specifying the y-axis variable name.}

\item{method}{Correlation method. One of `"spearman"` or `"pearson"`.
Default is `"spearman"`.}

\item{group_var}{Optional character string specifying a grouping variable
for point color mapping. Default is `NULL`.}

\item{use}{A character string specifying how to handle missing values.
One of `"complete.obs"` or `"pairwise.complete.obs"`. Default is `"complete.obs"`.}

\item{add_smooth}{Logical. If `TRUE`, add a linear regression line with
confidence interval. Default is `TRUE`.}

\item{smooth_method}{Method for smoothing line. Default is `"lm"`.}

\item{point_size}{Numeric. Size of points. Default is `2.5`.}

\item{alpha}{Numeric. Point transparency. Default is `0.8`.}

\item{show_stats}{Logical. If `TRUE`, annotate the plot with correlation
statistics. Default is `TRUE`.}

\item{digits}{Integer. Number of digits for correlation coefficient and
p-value formatting. Default is `3`.}

\item{annotation_x}{Numeric between 0 and 1 giving the x position of the
annotation in npc coordinates. Default is `0.05`.}

\item{annotation_y}{Numeric between 0 and 1 giving the y position of the
annotation in npc coordinates. Default is `0.95`.}

\item{xlab}{Optional x-axis label. Default is `NULL`, which uses `x`.}

\item{ylab}{Optional y-axis label. Default is `NULL`, which uses `y`.}

\item{title}{Optional plot title. Default is `NULL`.}
}
\value{
A ggplot object.
}
\description{
Create a scatter plot for a single pair of variables and annotate the plot
with the correlation coefficient, p-value, and sample size.
}
\details{
This function is intended for sample-level data, typically after thermal
feature extraction, repeated-measurement aggregation, and optional merging
with external phenotypic or clinical data.
}
\examples{
set.seed(123)
df <- data.frame(
  mean_temp = rnorm(10, 35, 0.5),
  body_weight = rnorm(10, 25, 2),
  group = rep(c("ND", "ND4"), each = 5)
)

p <- plot_thermal_correlation(
  data = df,
  x = "mean_temp",
  y = "body_weight",
  method = "spearman",
  group_var = "group"
)
print(p)

}
